#include <stdio.h>

int main(void) {
    int num, t=1;
    printf("Give me one number and I'll tell you its divisors\n");
    scanf("%d", &num);
    while (num>=t) {
        if (num%t==0) {
            printf("%d\n", t);
        }
        t++;
    }
    if(num%1==0 && num%num==0 && num%2!=0 && num%3!=0) {
        printf("Your number is a prime");
    }
    return 0;
}
